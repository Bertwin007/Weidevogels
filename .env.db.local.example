# Kopieer op de server naar .env.db.local (staat in .gitignore, nooit committen):
#   cp .env.db.local.example .env.db.local
#   nano .env.db.local
#
# Gebruik de MySQL-gegevens uit Plesk → Databases.

DB_DATABASE=greidefugels
DB_USERNAME=greidefugels
DB_PASSWORD=plak_hier_plesk_db_wachtwoord
